#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@dynatrace/react-native-plugin/scripts/Instrument.js" "$@"
  ret=$?
else 
  node  "$basedir/../@dynatrace/react-native-plugin/scripts/Instrument.js" "$@"
  ret=$?
fi
exit $ret
